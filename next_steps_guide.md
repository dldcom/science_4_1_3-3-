# 화산 타이쿤 - 다음 작업 가이드 (TODO List)

다른 환경에서 `git clone` 후 작업을 이어나가실 수 있도록, 현재 게임의 진행 상황과 앞으로 구현해야 할 작업들을 정리한 문서입니다.

## 1. 현재 구현 완료된 사항
* **기본 맵 시스템**: 3000x3000px의 넓은 맵, 터치/마우스 드래그를 통한 화면 패닝(이동) 기능.
* **건축 및 충돌 판정**: 상점에서 건물을 드래그 앤 드롭으로 건설하며, 화산이나 다른 건물과 겹치지 못하도록 충돌 판정(`checkOverlap`, `isCollidingLocal`) 적용 완료.
* **이미지 투명화 에셋 파이프라인**: 배경이 투명한 2.5D 정면 뷰 이미지(돌담, 조각상, 주상절리, 온천, 연못, 화덕, 농장 등) 적용 완료 및 `remove_bg.py` 파이썬 스크립트를 통한 후처리 프로세스 확립.
* **특수 상호작용 로직**: 
  * 마그마 돌 화덕: 클릭 시 고구마(🍠) 1개를 소모하여 군고구마(🍕) 굽기 이펙트 적용.
  * 화산재 농장: 클릭 시 화산재 자원을 소모하여 '비옥한 화산재 농장'으로 업그레이드 및 고구마 수확 시간 단축(30초 -> 15초).

---

## 2. 앞으로 해야 할 작업 (TODO List)

### 🎨 2-1. 남은 건축물 이미지 에셋 생성
아직 이모티콘(텍스트)으로 남아있는 건축물들의 이미지를 생성하고 `index.html`의 `items` 배열에 `img: '파일명.png'` 속성을 추가해야 합니다. (작성해드린 `image_generation_guide.md` 참고)

* **2타일(2x2) 크기**
  * `sand` (검은 모래 해변 타일 🏖️)
* **3타일(3x3) 크기**
  * `power` (지열 발전소 🏭)
  * `crater` (분화구 호수 타일 💧)
  * `garden` (화산재 텃밭 타일 🌱)

### 💻 2-2. 게임 로직 및 UI 개선
1. **퀘스트 완료 알림 (시각적 피드백)**
   * **현재 문제**: 퀘스트 조건을 달성해도 시각적으로 화려한 알림이나 팝업이 없습니다.
   * **구현 목표**: `renderQuests()`나 틱(Tick)이 돌아가는 `setInterval` 내부에서 `quest.condition()`이 처음 `true`가 될 때 폭죽이 터지거나 중앙에 큰 모달 창으로 "퀘스트 달성!"이 뜨도록 로직을 추가해야 합니다.
2. **카메라 패닝과 아이템 획득 드래그 겹침 개선**
   * **현재 상황**: 바닥을 드래그해서 맵을 이동하는 로직과 자원을 마우스(터치)로 훑어서 먹는(`isSweeping = true`) 로직이 간헐적으로 충돌할 수 있습니다.
   * **개선 아이디어**: 자원을 수집할 때 화면이 움직이지 않도록 하거나, '이동 모드'와 '건설/수집 모드'를 분리하는 토글 버튼을 UI에 추가하는 방안을 고려해 보세요.
3. **새로운 건물 상호작용 추가**
   * 농장 업그레이드와 화덕 굽기를 응용하여, **지열 발전소**를 클릭하면 화산 가스를 소모해 엄청난 양의 전기를 일시적으로 생산하거나 관광객을 끌어모으는 등의 상호작용을 추가할 수 있습니다.

### ⚙️ 2-3. 리소스 밸런싱
* 건물의 구매 비용(Cost)과 화산 폭발 시 떨어지는 자원의 양, 퀴즈 정답 시 획득하는 자원의 양 등 게임 내 경제 밸런스(`index.html` 상단 `items` 딕셔너리의 `cost` 및 `erupt()` 함수의 수치)를 테스트하며 조정해야 합니다.
